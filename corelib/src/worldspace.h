#ifndef GEOEMTRYBUF_H
#define GEOEMTRYBUF_H

#include "obj.h"

namespace e8 {

/**
 * @brief The if_world_space class
 */
class if_world_space : public if_obj_actuator {
  public:
    if_world_space() = default;

  private:
};

} // namespace e8

#endif // GEOEMTRYBUF_H
